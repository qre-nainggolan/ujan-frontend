export interface UserProfile {
  nama_User: string;
  userID: string;
  user_Profile?: string;
  registering?: string;
  fingerprint: string;
  expired_Password: string;
  approval_Notes: string;
  lastChange: string;
  password: string;
  retypePassword: string;
  status: string;
  displayName: string;
}
