const agent_mock = {
  Package: {
    getPackage: jest.fn(),
    getPackageTest: jest.fn(),
    submitQuestion: jest.fn(),
  },
  Account: {
    getUserProfile: jest.fn(),
  },
};

export default agent_mock;
